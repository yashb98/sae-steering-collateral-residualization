# Protocol amendments and implementation checks

18 September 2026, before any development or test generations: the initial discovery set with four adjective contrasts yielded only 24 features after the stated firing-frequency and exclusion filters. Expanded discovery to twelve positive/negative adjective pairs across the same eight subjects (96 paired sentences). Development prompts, test prompts, sample seeds, feature-scoring method and success criteria remain as originally written. The initial prompt file is retained as prompts_v0_discovery4.json. No generated behavioral outcomes informed this change.

Before behavioral generation: the expanded discovery set yielded 28 candidates at a 10% positive-context firing cutoff and 45 at a 3% cutoff. Set the discovery firing cutoff to 3% (at least three of 96 positive sentences), keep the original independent corpus-frequency band, and evaluate up to 48 available candidates. This admits sentiment-word-specific candidates while retaining a repeated-activation requirement. No generated outcomes informed this amendment.

## Interrupted-run recovery

18 September 2026, continuation session: GPT-2 development and test were complete; Gemma had 1,392 of 2,348 development outputs and no frozen selection. The interrupted Gemma JSONL ended in 4,088 NUL bytes after its last complete newline. The original file was backed up locally and only those bytes were removed. All 1,392 complete records were retained byte for byte, verified by a prefix SHA256. See RESUME_2026-09-18.json for the record and backup hash.

Gemma resumed with the same run.py, prompts, protocol, candidates and package environment, using batch size 64 as in the preceding interrupted run. Development finished before feature selection and held-out generation. The resumed session added provenance fields and a saved source snapshot for the secondary judge, independent output-grid and score checks, and report figures. The frozen generation runner, selection rules, scoring criteria and secondary assessment protocol were unchanged. No held-out outcome was used to change the experiment.

The completed Gemma audit exposed a verifier precision error: the checker read saved float32 candidate predictors as float64 and manually standardized them, changing predictions and a few near-tied ranks. This was especially visible for Gemma's nearly constant decoder norm. Reconstructing the original float32 inputs and applying the fitted StandardScaler reproduces both score vectors to within 4e-15 in both models, with every rank identical. The verifier now requires absolute error at most 1e-10 instead of using the previous loose rounding bound. Candidate scores, selections and generated outputs were not changed. A regression test covers this dtype round trip.
